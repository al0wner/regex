The `.conf` files under `./confs` should be placed at `$SDE_INSTALL/share/p4/targets/tofino`.

After compiling dfa.p4 and halfsiphash.p4, put the output directory in `$SDE/install/share/tofinopd/EXAMPLE_NAME`.
